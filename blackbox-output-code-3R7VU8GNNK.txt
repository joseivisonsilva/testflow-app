// app/modules/page.tsx (apenas as mudanças principais)
'use client'
// ... imports anteriores ...

export default function ModulesPage() {
  // ... código anterior ...
  
  if (authLoading || loading) {
    return (
      <div className="min-h-screen bg-gray-50 py-12">
        <div className="container mx-auto p-6">
          {/* Seu skeleton anterior */}
        </div>
      </div>
    )
  }

  return (
    <>
      <AppHeader />
      <div className="min-h-screen bg-gray-50 pb-12">
        <div className="container mx-auto p-6">
          {/* Seu conteúdo anterior */}
          
          {/* Badge de permissão no topo */}
          <div className="mb-6 p-4 bg-white rounded-xl shadow-sm border">
            <div className="flex items-center gap-3">
              <div className="flex -space-x-2">
                {permissions.avatarUrl ? (
                  <img
                    src={permissions.avatarUrl}
                    alt={permissions.fullName || ''}
                    className="w-10 h-10 rounded-full border-2 border-white shadow-md"
                  />
                ) : (
                  <div className="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white font-bold text-sm shadow-md">
                    {permissions.fullName?.charAt(0) || '?'}
                  </div>
                )}
              </div>
              
              <div>
                <p className="font-semibold text-gray-900">
                  {permissions.fullName} ({permissions.role})
                </p>
                <p className="text-sm text-gray-500">{permissions.email}</p>
              </div>
              
              <div className="ml-auto">
                {permissions.role === 'VIEWER' && (
                  <span className="px-3 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                    👁️ Somente visualização
                  </span>
                )}
                {permissions.role === 'EDITOR' && (
                  <span className="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs font-medium rounded-full">
                    ✏️ Sem exclusão
                  </span>
                )}
                {permissions.role === 'ADM' && (
                  <span className="px-3 py-1 bg-green-100 text-green-800 text-xs font-medium rounded-full">
                    👑 Administrador
                  </span>
                )}
              </div>
            </div>
          </div>

          {/* Resto do seu conteúdo anterior */}
        </div>
      </div>
    </>
  )
}